meta {
  name: Delete Account
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/auth/delete-account
  body: json
  auth: none
}

body:json {
  {
    "userId": "{{userId}}",
    "type": "soft"
  }
}
