meta {
  name: Reset Password
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "email": "john@example.com",
    "newPassword": "newpassword123"
  }
}
